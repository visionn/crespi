import styled from 'styled-components';
export const Container = styled.div`
  position: relative;
  height: 50vh;
  webkit-clip-path: inset(0 round 0.5rem 5rem);
  clip-path: inset(0 round 0.5rem 5rem);
  margin: 1rem 0;
`;
